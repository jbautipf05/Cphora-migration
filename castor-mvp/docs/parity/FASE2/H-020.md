# H-020 — Badges TIPO con texto y colores correctos · P2

**Módulo:** Clientes → tabla → columna TIPO · **Prioridad:** P2

## Diagnóstico
React renderizaba `<Chip variant={TIPO_LEAD[r.tipo]}>{r.tipo}</Chip>` → texto crudo en MAYÚSCULAS
("LEAD"/"INSTITUCIONAL") con colores lead→azul, institucional→dorado.
Demo6 (`Castor_Dashboard_Demo6.html:4312`): institucional → `badge-info` (azul) "Institucional";
resto → `badge-ok` (verde) "Persona", con mayúscula inicial (sin uppercase).

## Cambio
`src/views/Clientes.jsx` — render de la columna TIPO ahora:
- `institucional` → `<Chip variant="info">` (azul) texto "Institucional"
- resto → `<Chip variant="ok">` (verde) texto "Persona"
- `uppercase={false}` (mayúscula inicial).

NO se tocó el mapa compartido `TIPO_LEAD`: la tabla de **Leads** usa otro estilo en Demo6
(gold/info + texto "Lead", `Demo6:3874`), así que el cambio es inline solo en Clientes.

## Verificación (Preview MCP, :5173)
- Persona → color `rgb(110,231,183)` (emerald), bg emerald 0.2 ✓
- Institucional → color `rgb(125,211,252)` (sky), bg sky 0.2 ✓
- Textos "Persona"/"Institucional" con mayúscula inicial ✓

## Edge / caveats
- El badge del **encabezado del detalle** (Clientes.jsx:224) sigue usando `TIPO_LEAD` → se ajusta
  en H-024 (rediseño de encabezado). No es parte de H-020 (columna de tabla).

## Estado: ✅ CERRADO
