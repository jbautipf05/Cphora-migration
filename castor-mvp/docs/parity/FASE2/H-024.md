# H-024 — Detalle de cliente: nombre sin jerarquía visual · P2

**Módulo:** Clientes → detalle → encabezado del perfil · **Prioridad:** P2

## Diagnóstico
React usaba el título de la `SlidePanel` para el ID+badge (renderizado grande/blanco) y el
subtítulo para el nombre (pequeño, muted) → jerarquía invertida. Demo6 (`Castor_Dashboard_Demo6.html:4347-4353`):
fila [ID (mono dorado) + badge + VIP] arriba, y debajo el nombre grande dorado (`h2 gold-title`).

## Cambio
`src/views/Clientes.jsx` — encabezado de la `SlidePanel`:
- `title` = fila inline: ID `font-mono text-xs text-brand-gold` + `<Chip variant={ok|info} uppercase={false}>`
  ("Persona" verde / "Institucional" azul, coherente con H-020) + badge `VIP` (gold) si `estadoCli==='vip'`.
- `subtitle` = `<span className="text-lg font-bold text-gold-accent">{name}</span>` (nombre grande dorado).
- Eliminado import sin uso `TIPO_LEAD` (eslint 0).

## Verificación (Preview MCP, :5173)
- Fila ID: "C-001 · Persona". Nombre "Jose Ramírez" → `color rgb(201,169,97)`, `font-size 18px`, `font-weight 700`.
- `eslint src/views/Clientes.jsx` → exit 0.

## Edge / caveats
- Badge VIP aparece solo para clientes con histórico ≥ $20M (`estadoCli==='vip'`), igual que Demo6:4350.
- El badge de tipo del encabezado quedó alineado con la columna de la tabla (H-020): Persona verde / Institucional azul.

## Estado: ✅ CERRADO
