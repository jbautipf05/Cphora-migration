# H-035.3 — Modal "Nueva venta": Registro de pago + caja de liquidación · P0 (parte 3/3)

**Módulo:** Ventas → modal Nueva venta → bloque Pago · **Prioridad:** P0

## Diagnóstico
Faltaba el bloque de pago y la liquidación. Demo6 (`Castor_Dashboard_Demo6.html:5647-5671`):
contenedor con borde dorado "💰 Registro de pago" (Monto recibido / Método / Cuenta receptora /
Referencia / Soporte) + caja de liquidación (Subtotal stock, Subtotal producción, Descuento total, Total venta).

## Cambio
`src/components/NuevaVentaModal.jsx`:
- **Registro de pago** (borde dorado): Monto recibido*, Método* (`PAY_METHODS`), Cuenta receptora*
  (`bankAccounts`), Referencia / comprobante*, Soporte de pago (botón "📎 Adjuntar soporte" + "Sin adjuntar").
- **Monto recibido** sincroniza con **% Anticipo** (bidireccional): por defecto = `total × paid%`; al editarlo
  manualmente queda fijo (`payManual`).
- **Caja de liquidación**: `subStock`, `subProd`, `discTotal` (en rojo, con −), `total` (dorado).
- Prop `bankAccounts` añadida.
`src/views/Ventas.jsx` — `submitSale`:
- `paid%` del pedido se **deriva del pago realmente registrado** (`payAmount / total`), no de un % suelto.
- Si `payAmount > 0`: crea registro `PAYMENTS` (con método/cuenta/referencia/soporte) + asiento de cobro
  (`postCustomerCollection`). Esto **respalda el paid% con un PAG real** (mitiga EX-F2-02 en ventas nuevas).

## Verificación (Preview MCP, :5173)
- Bloque pago + liquidación presentes (Subtotal stock/producción, Descuento total, Total venta).
- Monto recibido auto = $1.425.000 (50% de $2.850.000); sync bidireccional OK.
- **E2E:** crear venta "Pago Test E2E" (Butaca Serenity) → fila **P-1254** (Total $2.85M, Saldo $1.425M, 50%) +
  **registro PAG-505 de $1.425.000** respaldando `order.paid=50`. ✅
- eslint exit 0. Datos de prueba limpiados.

## Edge / caveats
- El soporte se simula con un nombre de archivo (igual que Demo6 usa `prompt`); no hay subida real (alcance).
- Los campos de origen (CIERRE DE VENTA, ABONO A BANCOS) se guardan en el pedido aunque aún no se muestren
  en otras vistas.

## Estado: ✅ CERRADO (3/3 — H-035 COMPLETO)
