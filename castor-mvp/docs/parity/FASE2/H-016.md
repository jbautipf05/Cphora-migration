# H-016 — Tarjetas KPI superiores: reordenamiento y nuevos campos · P1

**Módulo:** Clientes → tarjetas de métricas superiores · **Prioridad:** P1

## Diagnóstico
React: orden Total → **Institucionales** → Con procesos activos → Postventa, sin subtítulos ni
iconos, y "Postventa pendiente" = 3 (incorrecto, sumaba todos los seguimientos no-resueltos).
Demo6 (`Castor_Dashboard_Demo6.html:4256-4264`): Total → Con procesos activos → **VIP** → Postventa,
con subtítulos e iconos; `postPend` cuenta CLIENTES con ≥1 seguimiento `pendiente`.

## Cambio
`src/views/Clientes.jsx`:
- **Cálculo `kpis`**: eliminado `institucionales`; agregado `vip = totals.filter(s => s.historico >= 20_000_000)`;
  `pendientesPostventa` ahora = nº de clientes con algún postventa `estado === 'pendiente'` (antes sumaba
  `postventaAbiertos`). Dep `postSales` añadida.
- **Tarjetas** (orden y estilo Demo6):
  1. Total clientes — "En la base" — `IconUsers` — #C9A961
  2. Con procesos activos — "Lead/Cot/Venta/Postventa" — `IconCheck` — #3b82f6
  3. VIP — "Histórico ≥ $20M" — `IconBadge` — #f59e0b
  4. Postventa pendiente — "Seguimientos abiertos" — `IconPhone` — #ef4444 si >0, si no #10b981
- Import de iconos añadido. eslint exit 0.

## Verificación (Preview MCP, :5173)
- 4 tarjetas en orden correcto, todas con icono (svg) y subtítulo:
  TOTAL CLIENTES=4 ("En la base"), CON PROCESOS ACTIVOS=4, VIP=0 ("Histórico ≥ $20M"),
  **POSTVENTA PENDIENTE=1** (corregido). 
- VIP=0 es correcto: ningún cliente del seed alcanza $20M (Adriano 18,9M es el máximo) — coincide con Demo6.

## Edge / caveats
- "Institucionales" se eliminó por completo (Demo6 no lo tiene); el filtro Tipo sigue permitiendo filtrar institucionales.
- El badge VIP de la tabla/encabezado usa la misma regla (≥$20M); hoy 0 clientes VIP.

## Estado: ✅ CERRADO
