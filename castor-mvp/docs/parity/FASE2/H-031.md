# H-031 — Filtros incompletos y faltante de Asesor · P1

**Módulo:** Ventas → barra de filtros · **Prioridad:** P1

## Diagnóstico
React tenía filtros Estado/Tipo/Medio/PDV pero faltaba el selector de **Asesor**. Demo6
(`Castor_Dashboard_Demo6.html:5268-5271`) incluye un select de Asesor (derivado de las órdenes).

## Cambio
`src/views/Ventas.jsx`:
- Estado `asesor`/`setAsesor`.
- `asesores = [...new Set(orders.map(o => o.asesor))].sort()` (igual que `asesoresV` de Demo6).
- Filtro en `rows`: `if (asesor && o.asesor !== asesor) return false;` (+ dep en useMemo).
- `<SelectFilter ... allLabel="Todos los asesores" />` añadido tras PDV.
- `ClearFiltersButton` incluye `asesor` en `active`/`onClear`.

## Verificación (Preview MCP, :5173)
- Selector Asesor presente con opciones: Todos los asesores / Alexander Vivas / Thalia Cifuentes.
- **Test funcional:** seleccionar "Thalia Cifuentes" → filas 5 → 2 (ambas Thalia). Reset → vuelve a 5. ✅

## Edge / caveats
- Los demás selectores ya eran compactos (`SelectFilter`); no requerían cambio adicional. El contador
  "N pedidos" del toolbar se mantiene (no flagged); la reestructuración a grupos izquierda/derecha
  + botones es parte de H-032/H-033.

## Estado: ✅ CERRADO
