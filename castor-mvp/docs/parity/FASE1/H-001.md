# H-001 — Botón "Nuevo" del header con menos opciones que Demo6 · P0

## 1. Diagnóstico (Demo6 vs React)
- **Demo6** (inspeccionado en vivo): `+ Nuevo` abre un **modal** `modal-quick-create`
  ("Crear nuevo") con **9 opciones**, cada una abre el formulario correspondiente:
  👤 Lead→openLeadForm() · 📄 Cotización→openQuoteForm() · 💰 Pago→openPaymentForm() ·
  🛡 Garantía→openWarrantyForm() · 📦 Nuevo producto→openProductInnovForm() ·
  ✓ Terminado→openFinishedForm() · 🧑 Empleado→openEmployeeForm() ·
  🏦 Cuenta bancaria→openBankForm() · 📞 Postventa→openPostSaleForm().
- **React** (antes): **dropdown** con 4 opciones que sólo navegaban (`setView`).

## 2. Cambio aplicado (decisión del usuario: modal + abrir formularios)
- **`src/components/Header.jsx`**: el botón abre un **Modal "Crear nuevo"** (espejo
  del modal de Demo6) con las **9 opciones** en orden y con emoji/labels iguales.
  Cada opción usa el intent `pendingForm` para abrir el form en la vista destino.
- **Mecanismo `pendingForm`** (ya existía para cotización): el Header setea
  `{type}` y navega; la vista destino consume el intent al montar/cambiar y abre su
  modal, luego limpia el intent. Se añadió el consumidor en **5 vistas**:
  - `Leads.jsx` → `{type:'lead'}` abre "Nuevo lead".
  - `Garantias.jsx` → `{type:'garantia'}` abre "Nueva garantía".
  - `Innovacion.jsx` → `{type:'producto'}` abre "Nuevo producto".
  - `RRHH.jsx` → `{type:'empleado'}` abre "Nuevo empleado".
  - `Postventa.jsx` → `{type:'postventa'}` abre "Registrar contacto directo".
  - `Cotizaciones.jsx` → `{type:'quote'}` (ya estaba) abre "Nueva cotización".
- **Gaps (opción A — navegar):** 3 opciones cuyo form de creación no existe aún en
  React, navegan al módulo sin abrir modal:
  - 💰 Pago → `ventas` (el pago se registra por pedido).
  - ✓ Terminado → `inventario`.
  - 🏦 Cuenta bancaria → `config-nomina` (se administran ahí).
  (Pre-existente: se quitó un import `nowISO` sin uso en Postventa.jsx.)

## 3. Verificación (en vivo, Preview MCP)
- "+ Nuevo" → **Modal "Crear nuevo"** con **9 ítems** en orden exacto. ✅
- Aperturas de formulario verificadas (3 vistas distintas, mismo mecanismo en las 6):
  Empleado→"Nuevo empleado" · Garantía→"Nueva garantía" · Postventa→"Registrar
  contacto directo". ✅
- Gap verificado: Pago → vista Ventas, **sin** modal de creación (navegación). ✅
- `eslint` 0 errores · `vite build` OK. ✅

## 4. Screenshot antes/después
- **Antes:** dropdown con 4 opciones (navegaban). **Después:** Modal "Crear nuevo"
  con 9 opciones que abren el formulario (capturas en el chat).

## 5. Edge cases probados
- Modal cierra con ✕ / backdrop / Cancelar de la vista destino. ✅
- Abrir un opener estando ya en su propia vista: el intent usa dep `[pendingForm]`
  (no sólo mount), así que igual abre el form. ✅
- Las 3 opciones gap navegan sin error y no abren modal. ✅
- Orden y labels = Demo6. ✅

## 6. Estado
✅ **Resuelto** (modal + 6 formularios en contexto; 3 gaps por opción A del usuario).

## 7. Caveats / pendientes
- Los **3 forms faltantes** (Pago suelto, Registrar terminado, Cuenta bancaria) no
  existen en React; quedan como hallazgos nuevos para una fase posterior
  (ver `HALLAZGOS_EXTRA.md` EX-03..EX-05). Hoy navegan (opción A, aprobada).
- Mapeo "Nuevo producto"→Innovación (handler `openProductInnovForm` de Demo6).
