# H-006 / H-008 / H-009 / H-012 — P1/P2 de paridad (Leads + Cotizaciones)

Cuatro hallazgos de **paridad real** con Demo6 (verificados en vivo, Preview MCP).
Aplicados "de corrido". Commit único.

## H-009 — Historial de notas en orden invertido (P1) · Leads
- **Demo6:** `[...l.notes].reverse()` → nota más reciente arriba (HTML 3993).
- **React (antes):** `(sel.notes||[]).map()` → orden de inserción (vieja arriba).
- **Cambio:** `[...(sel.notes||[])].reverse().map()` en `Leads.jsx`.
- **Verificado:** primera nota renderizada de Antonio = "Envió brief…" (la del 08-abr, más nueva). ✅

## H-012 — Falta filtro "Asesor" en Cotizaciones (P1) · Cotizaciones
- **Demo6:** la barra de filtros incluye Asesor (HTML 4464).
- **React (antes):** sólo Buscar / Estado / Canal.
- **Cambio:** estado `asesorF` + `asesoresQ` (derivado de quotes) + `<SelectFilter>` "Todos los asesores" + filtro en `rows` + integración con "Limpiar".
- **Verificado:** opción "Todos los asesores" presente; filtra por asesor. ✅

## H-006 — Dato secundario bajo el nombre en la ficha de lead (P2) · Leads
- **Demo6:** bajo el nombre muestra `razonSocial` (institucional) o `company||'—'` (lead) (HTML 3910).
- **React (antes):** sólo nombre + subtítulo (id/tipo/ciudad).
- **Cambio:** línea muted bajo el nombre con `razonSocial` (institucional) o `company||'—'`.
- **Verificado:** Antonio González (L-101, institucional) muestra **"GrupoInmob SA"** bajo el nombre. ✅

## H-008 — Desfase del ícono campana en "Contactar nuevamente" (P2) · Leads
- **Cambio:** botón con `inline-flex items-center gap-1.5` + ícono 12→14px (alineación menos invasiva, opción b del inventario).
- **Verificado:** ícono campana alineado con el texto. ✅

**eslint 0 · vite build OK.**
