# REG-H035-12 — Botón de eliminación de ítem (×)

**Fase:** 2.5.2 · **Prioridad:** P3 · **Bloque:** C · **Archivo:** `src/components/NuevaVentaModal.jsx`
**Estado:** ✅ Verificado + polish · lint + build OK.

## Verificación
El botón "×" ya existía, rojo y alineado a la derecha (`flex justify-between`), igual que Demo6
(`addSaleItemRow:5721`, `class="text-red-400 text-lg"`). Estaba esencialmente a paridad.

## Ajuste menor
Se añadió `leading-none` (centrado vertical más limpio del glifo) + `transition hover:text-red-300`
(afecto discreto al pasar el cursor). Enhancement no divergente; mantiene el estilo "× roja discreta".

## Calidad
`eslint` → 0 · `vite build` → OK. Cierra Bloque C.
