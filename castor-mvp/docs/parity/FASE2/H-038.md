# H-038 — Filtros simplificados y botones nuevos · P1

**Módulo:** Lista de Precios → barra de filtros · **Prioridad:** P1

## Diagnóstico
React tenía 5 inputs (buscador angosto + categoría + bodega + precio mín + precio máx), sin "Exportar PDF",
y el botón no existía / decía "+ Nuevo". Demo6: buscador ancho, Categoría, Bodega, "Limpiar", "Exportar PDF",
"+ Nuevo producto" (dorado) + texto sutil arriba.

## Cambio
`src/views/ListaPrecios.jsx`:
- **Texto sutil** arriba: "Catálogo completo con medidas finales, precio, descripción y ubicación."
- Filtros: buscador ancho (`flex-1`, placeholder "Buscar por nombre, SKU o descripción") + Categoría + Bodega.
  **Eliminados** Precio mín / Precio máx (estado + lógica de filtro + deps).
- Botones (derecha): "Limpiar" (si hay filtros), "📄 Exportar PDF" (btn-outline), "+ Nuevo producto" (btn-gold).
- Imports limpiados (`Toolbar`/`SearchBox`/`SelectFilter`/`ClearFiltersButton`/`Badge`/`wName` retirados; `useToast` agregado). eslint 0.

## Verificación (Preview MCP, :5173)
- Texto sutil presente; buscador ancho con placeholder correcto; labels Buscar/Categoría/Bodega; sin inputs de precio.
- Botones "📄 Exportar PDF" y "+ Nuevo producto" presentes. ✅

## Edge / caveats
- "📄 Exportar PDF" abre la exportación real en **H-040** (ahora stub con toast).
- "+ Nuevo producto" abre el modal en **H-041** (ahora stub con toast).
- `wName` se reintroducirá en H-039 si el rediseño de tarjetas lo necesita.

## Estado: ✅ CERRADO
