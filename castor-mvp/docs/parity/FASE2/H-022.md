# H-022 — Quitar contador "N clientes" sobre la tabla · P3

**Módulo:** Clientes · **Prioridad:** P3 (cosmético)

## Diagnóstico
React mostraba un `<span className="ml-auto text-sm text-muted">{rows.length} clientes</span>`
a la derecha del toolbar de filtros (`src/views/Clientes.jsx:211`). Demo6 no tiene ese contador.

## Cambio
Eliminado el `<span>` del contador dentro de `<Toolbar>`. No se tocó ningún filtro ni la lógica.

- `src/views/Clientes.jsx` — removida 1 línea (el span contador).

## Verificación (Preview MCP, :5173)
- CSS OK (`.btn-gold` computa `linear-gradient(135deg, rgb(201,169,97), rgb(168,141,75))`).
- Navegado a Clientes; ya NO existe ningún `<span>` con patrón `^\d+\s+clientes$` en el toolbar.
- El único "N Clientes" restante en el DOM es el badge de conteo del sidebar ("6\nClientes"), correcto.

## Edge / caveats
- Ninguno. Cambio puramente visual; los filtros siguen recalculando `rows` igual.

## Estado: ✅ CERRADO
