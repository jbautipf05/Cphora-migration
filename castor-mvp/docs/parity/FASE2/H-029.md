# H-029 — Caja de notas: funcionalidad React (mantener) + título a dorado · P2

**Módulo:** Clientes → detalle → "Notas y seguimientos" · **Prioridad:** P2

## Decisión explícita (inventario)
La funcionalidad de React (textarea + botón "+ Nota") es SUPERIOR a Demo6 (vacía). Se MANTIENE
tal cual. Acción única: cambiar SOLO el color del título a dorado.

## Cambio
`src/views/Clientes.jsx` — título "📝 Notas y seguimientos": `text-white` → `text-gold-accent`.
NO se tocó: textarea (`placeholder="Agregar nota..."`), botón "+ Nota", ni el handler `update('customers'…)`.

## Verificación (Preview MCP, :5173)
- Título "📝 Notas y seguimientos" computa `color: rgb(201,169,97)` (gold-accent).
- `textarea[placeholder="Agregar nota..."]` presente ✓; botón "+ Nota" presente ✓ (funcionalidad intacta).

## Edge / caveats
- Cambio puramente de color. La mejora React de notas queda preservada (mejora a mantener del inventario).

## Estado: ✅ CERRADO
