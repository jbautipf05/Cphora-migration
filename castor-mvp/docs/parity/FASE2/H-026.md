# H-026 — Tarjetas de métricas del cliente: títulos y colores · P2

**Módulo:** Clientes → detalle → tarjetas (Histórico, Saldo, Procesos activos) · **Prioridad:** P2

## Diagnóstico
React: títulos en MAYÚSCULAS sostenidas (`uppercase`), Histórico en blanco, Saldo en rosa/salmón
(`red-300`). Demo6 (`Castor_Dashboard_Demo6.html:4364-4366`): títulos con mayúscula inicial,
Histórico en dorado, Saldo en `red-400`/`emerald-400`.

## Cambio
`src/views/Clientes.jsx` — tarjetas del detalle:
- Títulos: quitado `uppercase` → "Histórico"/"Saldo"/"Procesos activos".
- Histórico: `text-white` → `text-gold-accent`.
- Saldo: `red-300`/`emerald-300` → `red-400`/`emerald-400` (coral/rojo vivo, verde si ≤0).
- Procesos activos: queda `text-white` (igual que Demo6).

## Verificación (Preview MCP, :5173)
- Títulos `text-transform: none`. Histórico `rgb(201,169,97)` (gold). Saldo `rgb(248,113,113)` (red-400). Procesos `rgb(255,255,255)`.

## Estado: ✅ CERRADO
