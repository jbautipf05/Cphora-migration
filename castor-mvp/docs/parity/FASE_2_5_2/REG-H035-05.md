# REG-H035-05 — Textarea de comentario por ítem (modal Nueva Venta)

**Fase:** 2.5.2 · **Prioridad:** P1 · **Bloque:** A · **Archivo:** `src/components/NuevaVentaModal.jsx`
**Estado:** ✅ Verificado + ajuste menor de paridad · lint + build OK.

---

## 1. Verificación
El inventario marcaba REG-05 como "parece ya existir → verificar paridad". Confirmado: el textarea de
comentario por ítem **ya existía** con el **placeholder idéntico** a Demo6 (`addSaleItemRow:5750`):
"Ej: preferencias, detalles especiales, alto del respaldo, etc.".

## 2. Único gap de paridad (corregido)
Demo6 muestra además un **label** encima del textarea: "Comentario (opcional, específico de este
producto)"; React no lo tenía (solo el placeholder). Se agregó el label (`label text-[10px]`) para
paridad visual exacta. Sin cambios de comportamiento.

## 3. Calidad
- `eslint` → 0 · `vite build` → OK. E2E live diferido (validación humana en PAUSA de Bloque A).
