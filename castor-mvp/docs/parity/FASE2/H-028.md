# H-028 — Sección Ventas/Pedidos del detalle: ID y badge · P1

**Módulo:** Clientes → detalle → bloque Ventas/Pedidos · **Prioridad:** P1
**Decisión de alcance (usuario):** COSMÉTICO — mantener id `PED-XXXX`, solo ajustar el badge.

## Diagnóstico
React: badge en MAYÚSCULAS con variante condicional (`entregado`→ok verde, resto→info). Demo6
(`Castor_Dashboard_Demo6.html:4381`) usa **siempre** `badge-info` (azul) en minúsculas con el
`estado` tal cual. Sobre el ID: Demo6 muestra `OP-XXXX` porque renombra el id al entrar a
producción (modelo de 2 niveles); React es plano y `PED-XXXX` es FK en 6+ tablas → **NO se renombra**
(decisión aprobada; ver nota de fallback del propio H-028 e INVENTARIO).

## Cambio
`src/views/Clientes.jsx` — badge de estado del bloque Ventas/Pedidos: `<Chip variant="info" uppercase={false}>{o.estado}</Chip>`
(antes: variante condicional + uppercase). El ID `{o.id}` (PED-XXXX) se mantiene.

## Verificación (Preview MCP, :5173, cliente C-001 Jose Ramírez)
- `PED-1248` y `PED-1253` conservan el prefijo `PED-`, en dorado (`rgb(201,169,97)`).
- Badges "entregado"/"pendiente_op": `text-transform: none` (minúsculas), color `rgb(125,211,252)` (info/azul).

## Edge / caveats
- Se descartó el verde para "entregado" para igualar Demo6 (siempre azul en este bloque).
- El modelo de 2 niveles P-/OP- (paridad real) quedó fuera de alcance por decisión del usuario
  (riesgo de integridad clase B2; ver plan Fase 2 y reporte de H-033 cuando se aborde).

## Estado: ✅ CERRADO
