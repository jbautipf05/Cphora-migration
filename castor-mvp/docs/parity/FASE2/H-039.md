# H-039 — Rediseño completo de tarjetas de producto · P1

**Módulo:** Lista de Precios → tarjetas individuales · **Prioridad:** P1

## Diagnóstico
Tarjetas React: nombre arriba + SKU debajo, badge categoría + pills de bodega de colores dispersos,
precio + "{n} und disp.". Demo6 (`Castor_Dashboard_Demo6.html:1716-1745`): SKU (mono gris) arriba,
nombre grande; un solo badge categoría ocre; bloque specs Medidas (dorado) + Bodega (color por sede);
fila cierre PRECIO COMERCIAL (dorado) + Stock (solo número).

## Cambio
`src/views/ListaPrecios.jsx` — tarjeta reescrita:
- SKU `font-mono text-[11px] text-muted` arriba; nombre `text-lg font-bold text-white` debajo.
- Un único `<Chip variant="gold">` (ocre) para la categoría (eliminadas las pills de bodega dispersas).
- Bloque specs 2 col: **Medidas** (gris) + valor `text-brand-gold-light` ("78 × 92 × 85 cm");
  **Bodega** (gris) + `code` con `style={{color: w.color}}` (CEDI dorado / Castor 43 azul / Volcanes morado).
- Fila cierre (border-top): "PRECIO COMERCIAL" (gris, uppercase tracking) + precio `text-xl text-gold-accent`;
  "Stock" + número (`text-lg`, blanco / ámbar si ≤min / rojo si 0). **Eliminado** "und disp.".

## Verificación (Preview MCP, :5173)
- Tarjeta "Butaca Serenity": SKU "BUT-SER-001" arriba, badge "BUTACAS", "Medidas 78 × 92 × 85 cm",
  "Bodega CEDI", "PRECIO COMERCIAL $ 2.850.000", "Stock 4". Sin pills, sin "und disp.". ✅

## Estado: ✅ CERRADO
