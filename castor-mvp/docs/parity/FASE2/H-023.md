# H-023 — Monto del cliente Carmiña Chapman desactualizado · P3

**Módulo:** Clientes → tabla → fila Carmiña Chapman (C-004) · **Prioridad:** P3

## Diagnóstico (con verificación del origen — flag del plan)
React mostraba histórico $7.340.000; Demo6 muestra $8.500.000.
- `customerTotalHist` de Demo6 (`Castor_Dashboard_Demo6.html:4213`) = `Σ order.total` por customerId.
- En el seed de Demo6, `PED-1245.total = null`; el valor se **deriva de la cotización** vía
  migración (`Demo6:625`): `o.total = Math.round(sub*(1-q.discount/100))`.
- COT-2002 (idéntica en React, `erpSeed.js:90`): items p6×1×7.400.000 + p5×4×650.000 = subtotal
  10.000.000; con `discount:15` → `round(10.000.000 × 0,85) = 8.500.000`.
- React hardcodeaba `PED-1245.total = 7.340.000`, inconsistente con su propia cotización.

## Cambio
`src/data/erpSeed.js:94` — `PED-1245.total`: `7340000` → `8500000` (alinea con la cotización COT-2002
y con el valor efectivo de Demo6).

## Verificación (Preview MCP, :5173, tras "Reset Data Demo")
- Fila Carmiña Chapman → columna Histórico = `$ 8.500.000`.
- Tipo "Persona", no VIP (8,5M < $20M, regla correcta), saldo recalculado = 8,5M × 40% = 3,4M.

## Edge / caveats
- El registro de pago `PAG-501` (9.180.000) es un dato histórico independiente del campo `total`
  (React modela el avance con `paid` %); el cambio de `total` no toca pagos ni la contabilidad
  (slices contables tienen su propio seed). Inconsistencia pago>total preexistente, igual que Demo6.

## Estado: ✅ CERRADO
