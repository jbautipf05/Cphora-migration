# H-104 — Innovación: Nuevo Producto con BOM y cálculo de costo

**Prioridad:** P0 · **ESTADO: ✅ YA SATISFECHO EN FASE 2/2.5** (residual: 1 decisión aplicada)

## Requerimiento del inventario
Modal "Nuevo producto" en Innovación (no en Lista de Precios) con: datos básicos, áreas que
recorre (multi-selección), BOM (insumos + cantidades), cálculo automático de costo y margen.

## Verificación contra el código real (`src/views/Innovacion.jsx`)

| Bloque pedido | Estado | Evidencia |
|---------------|--------|-----------|
| Datos básicos (nombre, desc, SKU, categoría, foto, bodega) | ✅ | modal [Innovacion.jsx:301-317](../../../src/views/Innovacion.jsx) |
| **Áreas que recorre** (multi-selección) | ✅ Checkboxes sobre `PROD_AREAS`, persiste `product.areas` | [L327-337](../../../src/views/Innovacion.jsx) · `toggleArea` |
| **BOM** (tabla insumos + cantidad) | ✅ Filas con selector de insumo + qty, agregar/eliminar | [L340-356](../../../src/views/Innovacion.jsx) |
| **Cálculo automático de costo** | ✅ `bomCost` suma `cost × qty` de cada insumo; se muestra en vivo | `bomCost` [L62-65](../../../src/views/Innovacion.jsx) · total [L357-360](../../../src/views/Innovacion.jsx) |
| **Margen** | ✅ `(price - cost) / price` en vivo | `formMargin` [L105](../../../src/views/Innovacion.jsx) |
| Modelo de datos | ✅ `products` ya tienen `bom:[{supplyId,qty}]` y `areas:[...]` y `cost`; `supplies` tienen `cost` | [erpSeed.js:32-59](../../../src/data/erpSeed.js) |

El modelo ya estaba completo — **no fue necesario extenderlo** (la condición de "pausar si
falta algo en el modelo" no aplicó).

## Cambio aplicado (residual)
El cliente pidió que el alta de producto esté **solo en Innovación, no en Lista de Precios**.
Se **quitó el botón "+ Nuevo producto" de Lista de Precios**
([ListaPrecios.jsx:155](../../../src/views/ListaPrecios.jsx), ahora removido). El modal simple
H-041 y su `emptyProduct` **se conservan en el código sin entry point público**
(`eslint-disable no-unused-vars` documentado) por si en un sprint futuro el cliente lo
vuelve a pedir. Lista de Precios queda como catálogo (filtros + edición + export PDF), sin
alta.

También se corrigió en el modal de Innovación el selector de bodega para mostrar solo PT
(ver [[H-102]]).

## DECISIÓN SIN INPUT DEL CLIENTE
Confirmada con el usuario en el arranque del sprint: **conservar el componente H-041** (no
borrarlo), solo retirar su botón disparador. *Defensa en demo:* el alta con BOM ya está en
Innovación; Lista de Precios es catálogo/precios.

## Auto-validación
- ESLint `Innovacion.jsx` + `ListaPrecios.jsx`: limpio (0 errores; `emptyProduct` con
  disable documentado).
- `vite build`: OK (exit 0).
- El modal de Innovación ya estaba operativo (reachable por el botón "+ Nuevo producto" de
  la vista y por el quick-create global del header → "Nuevo producto").
