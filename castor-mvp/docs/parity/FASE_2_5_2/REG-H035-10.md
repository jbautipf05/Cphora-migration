# REG-H035-10 — Dropdown de producto con precio + stock + filtrado por stock

**Fase:** 2.5.2 · **Prioridad:** P1 · **Bloque:** C · **Archivo:** `src/components/NuevaVentaModal.jsx`
**Estado:** ✅ Implementado · lint + build OK.

## Inspección Demo6 (`onSaleItemTypeChange:5805-5813`)
- Modo **Producción**: opciones = todos los activos, etiqueta `Nombre — $precio`.
- Modo **Stock**: dropdown **filtrado** solo a productos con disponibilidad, etiqueta
  `Nombre — stock: N — $precio`; placeholder "— Producto en stock —".

## Implementación
- Helpers `stockAvail(productId)` + `productOptions(tipo, selectedId)`.
- Select muestra opciones según `it.tipo`; en Stock filtra por `stockAvail>0` y agrega "stock: N";
  placeholder condicional. Mantiene visible el producto ya seleccionado aunque caiga fuera del filtro.
- React ya mostraba `Nombre — $precio` (el inventario decía "solo nombre", impreciso); el gap real
  era el **stock en la etiqueta** y el **filtrado en modo Stock** → cubierto.

## Calidad
`eslint` → 0 · `vite build` → OK. E2E live no disponible (Preview inoperante este sesión; ver CIERRE).
A validar a mano: ítem en Stock → dropdown solo p1/p2/p5/p7 (con stock) y "stock: N"; en Producción → todos.
